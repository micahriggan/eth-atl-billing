{
  "name": "eth-atl-billing-contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile:contract": "cd ./blockchain && ../node_modules/.bin/truffle compile",
    "compile:contract:types": "./node_modules/.bin/typechain --target=web3-1.0.0 --outDir types blockchain/contracts/build/**/*.json",
    "compile": "npm run compile:contract && npm run compile:contract:types",
    "test": "cd ./blockchain && ./node_modules/.bin/truffle test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "truffle": "^4.1.14",
    "typechain": "^0.3.5"
  }
}
